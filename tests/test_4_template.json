{
	"{{#merge}}" : {
		"{{#each inputs}}" : {
			"task_{{name}}" : {
				"state" : "{{state}}"
			}
		}
	},
	"gather" : {
		"inputs": {
			"{{#each inputs}}" : {
				"id" : "task_{{name}}"
			}
		}
	}
}