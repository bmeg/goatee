{
    "data" : {
        "{{#merge}}" : {
            "{{#each inputs}}" : {
                "task_{{name}}" : {
                    "state" : "{{state}}"
                }
            }
        }
    }
}